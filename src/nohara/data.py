# ============================================================
# Global Data Registers for Nohara
# ============================================================

# --- Config ---
CELL_W = 8
CELL_H = 16

SCREEN_W = 1200
SCREEN_H = 800

# --- Camera (CellPos) ---
CAM = {
    "x": 0,
    "y": 0,
}

# --- Caret (CellPos) ---
CARET = {
    "x": 0,
    "y": 0,
}

# --- Background Text ---
# (cell_x, cell_y) -> single-character string
BG_TEXT = {}

# --- Runtime ---
RUNTIME = {
    "running": True,
}
